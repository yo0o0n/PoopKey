<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.poopkey.application.main.dao.ItemSensorUpdateDao">
    <update id="update">
        UPDATE item SET tissue = #{tissue}
        WHERE stall_id = #{stallId}
    </update>
</mapper>
